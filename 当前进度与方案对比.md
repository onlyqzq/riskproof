# RiskProof 实现进度与验收说明

> 更新日期：2026-07-27  
> 本文以仓库实际代码与自动化测试为准，不以设计设想或清单勾选代替实现。

## 标记说明

- ✅ 已实现，并有自动化测试或构建验证
- ⚠️ 已实现确定性边界，但受协议、部署环境或外部服务能力限制
- ❌ 未实现

---

## 一、数据流追踪（Provenance）

原文档中最大的缺口已经补齐。MCP Proxy 现在包含独立、可复用的
`ContextTracker` 与 `ProvenanceMapper`，不再把所有参数粗粒度标成 `mcp_tool`。

| 能力 | 当前状态 | 实现说明 | 代码位置 |
|---|---|---|---|
| 拦截 MCP 消息建立上下文索引 | ✅ | 追踪 `resources/read`、`prompts/get` 和成功的 `tools/call` 返回 | `packages/riskproof/src/provenance.ts`、`proxy-server.ts` |
| 参数值 → context entry 反查 | ✅ | 确定性精确子串匹配；短标量只允许全值匹配，避免 `1`/`true` 等误关联 | `ProvenanceMapper.mapArguments` |
| 自动标注 `agent_generated` | ✅ | 没有匹配上下文的参数明确标注为 `agent_generated` | `provenance.ts` |
| 来源 ID 语义命名 | ✅ | 生成 `webpage_1`、`email_1`、`customer_data_1`、`source_code_1` 等 ID | `ContextTracker.record` |
| MCP schema 投毒检测 | ✅ | 投毒 schema 不暴露给模型；直接调用仍由隔离缓存阻断 | `proxy-server.ts` |
| 有界内存与隐私保护 | ✅ | 256 条、单条 256 KiB、总计 2 MiB、10k 节点、64 层默认上限；公共诊断只返回摘要和 SHA-256 digest，不返回原文 | `CONTEXT_TRACKER_LIMITS` |
| 摘要/改写后的污点传播 | ✅ | `flows: [{from,to,via}]` 采用有界不动点传播，只能追加来源/污点，不能清除证据 | `types.ts`、`validation.ts`、`engine.ts` |
| MCP 派生关系元数据 | ✅ | 支持 `_meta.riskproof_flows`，并在运行时校验参数引用 | `proxy-server.ts` |
| 完整重建不可见 LLM 推理 | ⚠️ | 协议层无法观察模型内部推理；有损摘要必须由可信集成声明 `flows`，系统不会用模糊相似度猜测安全来源 | 设计边界 |

安全边界：上下文原文仅存在于有界内存索引中，不写入 proof、不进入日志，也不通过
`listContextEntries()` 返回。该接口只暴露 ID、kind、污点、字符数和内容摘要。

---

## 二、工具身份、任务授权与协议边界

| 能力 | 当前状态 | 实现说明 |
|---|---|---|
| 完整 Tool descriptor commitment | ✅ | canonical SHA-256 覆盖 name、description、input/output Schema、annotations、`_meta` 和未来字段 |
| Rug pull / late addition / pinned mismatch | ✅ | 进程内 TOFU 与 operator-pinned 两种模式；sticky quarantine，回滚旧描述符不会自动恢复 |
| 同快照工具重名 | ✅ | 在 `Map` 折叠之前检测，冲突定义全部不向规划模型暴露，直接调用同样 critical deny |
| Host-held Task Contract | ✅ | exact tool、descriptor digest、allowed provenance、expiry、全局/单工具预算 |
| 并发预算安全 | ✅ | 调用在 dispatch 前原子 reserve；失败/拒绝 abort；成功才 complete，防止并发读检查绕过 |
| 可信用户来源注入 | ⚠️ | 程序化 Host 可认证后写入共享 `ContextTracker.trusted_user`；CLI 自身不负责用户认证 |
| 结构化任务证据 | ✅ | `task_contract_matched` 把 contract/task/objective/descriptor digest 写入 proof 规则证据 |
| 语义 Task Alignment | ❌ | `objectiveDigest` 只做绑定，不实现 Siu 等人框架中的 `HTr/Ha` 理想 oracle |
| 双向 MCP request allowlist | ✅ | 未知 client request 默认 `-32601`；Sampling/Elicitation/Roots/custom server request 不下发客户端 |
| Response correlation / normalization | ✅ | 未匹配 response 丢弃；夹带 method 的合法 response 被规范化；initialize capabilities 收窄 |
| Remote MCP auth / 完整版本协商 | ❌ | 当前是 stdio 第一版协议防火墙；OAuth、DNS rebinding、远程身份仍属路线图 |

默认 TOFU 只证明“首次快照之后有没有变化”，不证明首次 Server 来源真实；descriptor
hash 也不证明后端代码按声明执行。任务合同证明机器可检查的结构授权匹配，不证明
Agent 的动作在语义上真的服务用户目标。这两个边界已经进入代码注释、README、
`SECURITY.md` 和研究演示，而不是留给口头解释。

---

## 三、污点推断与传播（Taint）

| 能力 | 当前状态 | 实现说明 |
|---|---|---|
| 语义来源 → 污点 | ✅ | 11 类 `TaintLabel` 均有对应语义来源关键字 |
| 参数值 → 污点 | ✅ | 覆盖 API Key、Secret、客户数据、PII、内部文档、源码、财务数据和患者数据 |
| 声明污点 | ✅ | 调用方仍可显式声明，运行时严格校验枚举和参数引用 |
| 三路合并与去重 | ✅ | 声明污点 ∪ 来源推断 ∪ 值检测 |
| 跨参数传播 | ✅ | `flows` 支持链式和循环图的有界单调传播 |
| Sink 标记 | ✅ | 邮件、HTTP、Shell、文件、数据库和浏览器参数均有 sink 定义 |
| 防 TOCTOU | ✅ | 传播和反查前先对输入做安全快照，拒绝 Proxy/getter/循环引用 |

当前 11 种标签：

```text
UNTRUSTED_WEB          来源自动推断
UNTRUSTED_EMAIL        来源自动推断
UNTRUSTED_TOOL_SCHEMA  来源/投毒检测自动推断
INTERNAL_DOC           来源 + 值检测
CUSTOMER_DATA          来源 + 值检测
PII                    来源 + 值检测
SECRET                 来源 + 值检测
API_KEY                来源 + 值检测
SOURCE_CODE            来源 + 值检测
FINANCIAL_DATA         来源 + 值检测
PATIENT_DATA           来源 + 值检测
```

正则检测是纵深防御，不替代组织级 DLP 或结构化数据分类器。所有正则命中都会进入
确定性策略，不使用 LLM 判断标签。

---

## 四、策略引擎与 policy-as-code

| 能力 | 当前状态 | 实现说明 |
|---|---|---|
| 内置规则 | ✅ | 21 条单次调用确定性匹配规则 + 独立工具链序列策略 + `defaultDecision` 配置兜底 |
| 规则优先级 | ✅ | Invariant > Capability > Deny > Review，最终取最严格决策和最高风险 |
| JSON/YAML 自定义规则 | ✅ | RE2JS 线性时间正则，限制规则数、深度、节点数、字符串与文件大小 |
| OPA/Rego 嵌入 | ✅ | 使用官方 `@open-policy-agent/opa-wasm`，加载预编译 Rego WASM |
| OPA 多模块聚合 | ✅ | 内置规则只计算一次；多个 OPA 模块结果单调聚合，不能降级已有决策 |
| OPA fail-closed | ✅ | 运行错误、超大结果、非法字段、未知参数引用均默认产生 critical deny |
| CLI / Proxy / HTTP 接入 OPA | ✅ | CLI `--opa-policy`，Proxy/HTTP `opaPolicies`，JS `evaluateWithOpa` |
| 真实 Rego → WASM 集成 | ✅ | OPA CLI v1.18.2 严格校验并编译示例 Rego，官方 JS runtime 实际执行命中/不命中路径及 proof 一致性 |
| 工具覆盖 | ✅ | `send_email`、`http_request`、`shell_exec`、`file_read`、`file_write`、`database_query`、`browser_action` |
| 数据库危险操作 | ✅ | DROP/TRUNCATE/ALTER/GRANT/REVOKE/DELETE/UPDATE 等变更默认阻断 |
| 不可信内容驱动可变更工具 | ✅ | 文件写入、数据库和浏览器操作受不可信来源影响时要求审批 |

OPA/Rego 采用预编译 WASM，而不是运行时调用外部 OPA HTTP 服务，因此保留本地执行、
低延迟和无网络依赖的安全属性。Rego 编译属于发布/部署阶段职责。

---

## 五、风险解释（Explainer）

| 能力 | 当前状态 | 实现说明 |
|---|---|---|
| 四段式审批卡片 | ✅ | 参数来源链、策略命中、后果、建议 |
| 精简日志格式 | ✅ | `formatCompact` |
| 规则解释数据库 | ✅ | 新旧内置规则均提供中英文 label/detail/consequence |
| 多语言一致性 | ✅ | `zh-CN` 与 `en`；标题、规则说明、后果和建议不再中英混杂 |
| LLM 润色 | ✅ | `formatPolishedCard` 接受厂商无关的 `ExplanationPolisher` |
| 润色安全边界 | ✅ | 只发送脱敏结构；超时/异常/空结果回退模板；ANSI 清洗与长度上限；润色文本不能改变决策 |
| 终端安全与排版 | ✅ | ANSI/控制字符清洗、CJK 显示宽度截断与换行、敏感值脱敏 |

RiskProof 不内置某一家模型的 API Key 或网络客户端。调用方可接入任意 LLM，但模型只
能生成附加说明；权威的 action/decision/risk 和确定性卡片始终保留在输出前部。

---

## 六、审计证明（Proof Store）

| 能力 | 当前状态 | 实现说明 |
|---|---|---|
| 结构化 Proof | ✅ | proof ID、工具、决策、风险、规则、证据、原因、时间与 trace |
| 原子不覆盖写入 | ✅ | 完整临时文件 → hard link 原子提交；并发时生成唯一后缀 |
| POSIX 权限 | ✅ | 目录 `0700`，文件 `0600` |
| 结构完整性校验 | ✅ | record、engineOutput、proof、rules、evidence、reason 交叉一致性 |
| 敏感值脱敏 | ✅ | 加密前仍先脱敏，降低密钥泄露后的数据暴露范围 |
| 静态加密 | ✅ | AES-256-GCM，96-bit 随机 IV，认证 tag，绑定格式/key ID/IV 的 AAD |
| 防篡改签名 | ✅ | HMAC-SHA-256，常量时间比较；签名覆盖 envelope 元数据和 payload |
| 密钥轮换读取 | ✅ | 当前写密钥 + 旧 decryption/verification keyring，按 SHA-256 key ID 选择 |
| 严格迁移模式 | ✅ | 可要求加密和/或签名；默认仍兼容旧版明文 JSON |
| 保留策略 | ✅ | `maxAgeDays`、`maxRecords`、显式 `prune()`、`pruneOnSave` |
| 损坏证据保护 | ✅ | 无法读取/验签/解密的文件报告有界诊断，但不会自动删除 |
| 多维度查询 | ✅ | tool/decision/action/risk/time/limit |

仍需外部系统承担的职责：KMS/HSM、远程复制、备份恢复、磁盘配额和容量报警、跨主机
授权，以及公开可验证的非对称签名。HMAC 是共享密钥完整性证明，不是不可抵赖签名。

---

## 七、运行时输入与配置验证

| 能力 | 当前状态 |
|---|---|
| 深度快照、plain object 限制、拒绝 Proxy/getter/循环引用 | ✅ |
| 32 层、10k 节点、2 MiB 总字符输入预算 | ✅ |
| tool/taint/capability/flow 枚举和交叉引用校验 | ✅ |
| 防原型污染和 credential-like 参数名注入 | ✅ |
| Config 1 MiB/10k 节点/16 层/256 规则限制 | ✅ |
| RE2 编译期校验，禁止回溯、lookaround 和 backreference | ✅ |
| OPA 输出 1 MiB/128 matches/16 KiB 单字符串限制 | ✅ |
| Proof envelope 大小、Base64、算法、key ID、IV/tag、签名严格校验 | ✅ |

---

## 八、多接入模式

| 模式 | 当前状态 | 说明 |
|---|---|---|
| CLI check | ✅ | RiskProof/Claude Code 输入、OPA WASM、明确退出码 |
| HTTP Server | ✅ | `/evaluate`、`/health`、`/ready`、body/CORS/timeout/trust boundary |
| MCP Proxy | ✅ | descriptor identity、task contract、双向方法防火墙、自动 provenance、评估、审批、proof、backpressure |
| JS/TS API | ✅ | 核心引擎、tracker/mapper、OPA、explainer、proof store 均导出 |
| Python SDK | ✅ | LangGraph 两阶段评估与批量审批 |
| Docker | ✅ | digest 固定的多阶段镜像；非 root、只读 rootfs、cap-drop、加密签名 proof、持久化与 SIGTERM 已真实 smoke |

---

## 九、测试与验证

截至本文更新时的本地基线：

```text
TypeScript 测试文件：12
自动化测试：301
状态：全部通过
```

新增覆盖包括：

- MCP resource → 后续参数的端到端自动 provenance 映射；
- 完整 descriptor canonicalization、TOFU/pinned identity、rug pull 回滚粘滞隔离；
- Host task contract 的 descriptor/source/expiry/budget、并发预留与 dispatch 成败结算；
- 双向 MCP request allowlist、server-initiated request 拒绝和 response 规范化；
- ContextTracker 有界内存、语义 ID、不暴露原文、短标量防误匹配；
- 11 类来源和值污点、链式派生传播；
- 数据库和可变更工具规则；
- OPA 合法决策、单调聚合、非法契约 fail-closed、结果复杂度上限；
- 中英文解释、LLM 输入脱敏、ANSI 清洗、异常回退；
- AES-GCM/HMAC 正常读取、篡改、错误密钥、keyring 轮换、严格模式；
- 年龄/数量保留和损坏证据不删除；
- 原有 180 项引擎、配置、安全、Proxy、HTTP 与 Proof 回归测试。

额外的发布级集成验证也已真实执行：

- `npm run test:opa`：使用 OPA CLI v1.18.2 对
  `examples/policies/production-deploy.rego` 执行 `check --strict` 和 WASM 编译；
  `OpaPolicyEngine.loadFile` 实际加载后，production 路径得到 `ask_approval/high`，
  staging 路径保持 `allow`，proof 交叉一致性通过；
- `docker build`：固定基础镜像
  `node:22.20.0-alpine3.22@sha256:dbcedd8a…`，构建成功；实构发现并修复 runtime
  stage 漏带 `@open-policy-agent/opa-wasm` 的真实发布缺陷；
- `npm run test:docker`：最终候选镜像 ID `sha256:877beebe…`，inspect size
  `57,436,484` bytes；验证非 root、只读 rootfs、cap-drop、no-new-privileges、
  400/413/415、危险调用阻断与脱敏、AES-256-GCM/HMAC-SHA-256、proof `0600`、
  容器重建后解密验签/持久化，以及 SIGTERM 后 exit 0；
- CI 与发布工作流会下载固定 OPA v1.18.2 静态二进制并校验 SHA-256，再运行相同
  OPA smoke；Docker job 和发布 job 复用相同的 hardened runtime smoke。

这些是 macOS 上 Docker Desktop Linux/aarch64 的本地发布证据；目标 Linux runner、
生产 proof 卷 hard-link 语义、远端 CI 和回滚演练仍需发布负责人执行，不能由本地
通过替代。

最终发布前仍应执行：

```bash
npm run verify
npm run test:opa
npm audit --audit-level=high
npm run test:docker
cd agent && uv sync --frozen --extra dev && uv run pytest -q
```

---

## 十、完成后的诚实总结

> RiskProof 已从“依赖外部喂入 provenance 的风险计算器”升级为 MCP/Agent 工具链的
> 确定性 reference monitor：具备完整工具描述符连续性、Host-held 任务合同、双向协议
> 边界、自动 MCP 上下文追踪、单调污点传播、跨工具序列策略、安全解释以及可保护的
> 审计 proof。

它仍然不是 sandbox、身份认证服务、完整 DLP、公开不可抵赖账本，也无法读取 LLM 的
隐藏推理。精确文本追踪不到有损摘要时，需要可信集成声明 `flows`。这些是明确的系统
边界，不再是原文档中“尚未实现”的代码缺口。
