# RiskProof 项目深度解析

## 一、项目一句话定位

**RiskProof 是 MCP/AI Agent 工具链的确定性 reference monitor：先验证工具身份，再
约束任务授权与数据来源，持续追踪跨工具权限跃迁，在真实副作用发生前 Allow、
Step-up 或 Block，并让每次裁决携带可审计证据。**

它不替代现有 Agent 框架，而是位于 Agent 与真实 MCP Server 之间。与普通 HITL 的
关键区别是：明确违规由确定性策略直接阻断，只有语义证据不足的真实例外才升级审批。
系统回答的不只是“Agent 要做什么”，而是“这是哪个版本的工具、谁授权了当前任务、
参数从哪里来、跨了什么权限边界、会把什么数据送到哪里”。

当前可现场运行：

```bash
npm run demo:research
```

演示不调用真实 LLM、不访问外网、不执行危险命令，依次重现 descriptor rug pull、
Host task contract 来源越权、EIT→PAT→NAT 外发、metadata SSRF/持久化和双向 MCP
协议绕过，并明确输出每一项的研究依据、规则证据与局限。

### 产品故事的六层结构

```text
Tool Identity / Manifest Continuity
              ↓
Trusted Task Contract
              ↓
Argument Provenance & Taint
              ↓
Per-call + Cross-tool Policy
              ↓
Bidirectional MCP Protocol Boundary
              ↓
Redacted Decision Proof / Future Execution Receipt
```

学术上，这条主线从 Saltzer & Schroeder 的 complete mediation / least privilege，
连接到 CaMeL/Fides 的 capability 与 information-flow control、Siu 等人的四个上下文
安全性质、ETDI/MPMA 的工具身份与选择完整性，以及 Parasites/AgentDojo/InjecAgent
的组合攻击与可复现实验。完整引用、数字分母和不可泛化边界见
`docs/research-foundations.md`。

---

## 二、由浅入深：技术实现拆解

### 第一层：核心数据模型（[types.ts](packages/riskproof/src/types.ts)）

整个系统的"血液"是类型系统。核心概念只有几个：

```
EngineInput（输入）              EngineOutput（输出）
├── tool: 工具名                  ├── action: allow | ask_approval | block
├── args: 工具参数                ├── decision: allow | require_approval | deny
├── provenance: 参数来源追踪       ├── riskLevel: low | medium | high | critical
├── taints: 污点标签              ├── matchedPolicies: 命中的策略列表
├── flows: 参数派生关系           ├── arguments: 增强后的参数（含来源+污点）
├── capability: 能力授权           └── proof: 结构化审计证明
├── invariants: 安全不变式
└── trace: 链路追踪
```

**设计亮点**：输入输出都是单一干净的数据结构，不做隐式转换。工具使用显式 TypeScript union type，覆盖邮件、HTTP、Shell、文件读写、数据库和浏览器操作；JSON 边界仍会做运行时枚举校验。

**11 种污点标签（TaintLabel）**：

- 不可信类：`UNTRUSTED_WEB`、`UNTRUSTED_EMAIL`、`UNTRUSTED_TOOL_SCHEMA`
- 敏感数据类：`CUSTOMER_DATA`、`PII`、`SECRET`、`API_KEY`、`SOURCE_CODE`、`FINANCIAL_DATA`、`PATIENT_DATA`
- 内部类：`INTERNAL_DOC`

---

### 第二层：参数级来源追踪与污点分析（[engine.ts](packages/riskproof/src/engine.ts) Part 1 & 2）

这是项目的第一个技术核心。系统通过**四种机制**识别和传播参数风险：

#### 机制 0：自动 MCP 上下文反查

Proxy 截获 `resources/read`、`prompts/get` 和成功的 `tools/call` 返回，使用有界
`ContextTracker` 建立内存索引。后续参数通过确定性精确子串匹配得到
`webpage_1`、`email_1` 等语义来源；无匹配值标为 `agent_generated`，不再依赖
调用方给所有参数手工标注粗粒度来源。

#### 机制 1：来源推断（Source → Taint）

```typescript
// engine.ts:47-51
const SOURCE_KIND_TO_TAINT = [
  // 不可信网页/邮件/schema + 内部文档/客户/PII/凭据/源码/财务/患者来源
];
```

无论来源是 MCP Proxy 自动反查，还是可信直接调用方声明
`provenance: { to: ["webpage"] }`，引擎都会推断 `to` 参数带有
`UNTRUSTED_WEB` 污点。**关键词匹配**：source ID 中包含
"webpage"/"email"/"tool_schema" 即触发。

#### 机制 2：值检测（Value → Taint）

```typescript
// engine.ts:64-77
const SENSITIVE_PATTERNS = [
  { label: "API_KEY",    patterns: [/sk-[a-zA-Z0-9_-]{20,}/i, /Bearer\s+[a-zA-Z0-9._\-]{20,}/i] },
  { label: "SECRET",     patterns: [/\bapi[_-]?key.../, /\bsecret.../, /\btoken.../, /\bpassword.../] },
  { label: "CUSTOMER_DATA", patterns: [/\bcustomer\b/i, /\bclient\b/i, /客户/] },
  { label: "PII",        patterns: [邮箱正则, 电话号码正则] },
];
```

即使调用方没有声明污点，引擎也会**扫描参数值本身**，自动检测 API Key、密钥、客户数据、PII、内部文档、源码、财务和患者数据。

#### 机制 3：污点富化（Merge）

```typescript
// engine.ts:111-131
function enrichTaints(args, inputTaints) {
  // 合并：声明污点 ∪ 来源推断污点 ∪ 值检测污点
}
```

**面试要点**：这不是简单的字符串匹配，而是一个**多层污点传播模型**的 MVP 实现。借鉴了学术界的 Information-Flow Control（如 Microsoft FIDES）和 NeuroTaint 的思路，但收敛到 approval 决策增强这个具体场景。

此外，`flows: [{from,to,via}]` 用有界不动点算法处理摘要、改写和链式派生；传播
只能追加来源和标签，不能删除已有证据。

---

### 第三层：确定性策略引擎（[engine.ts](packages/riskproof/src/engine.ts) Part 3）

这是项目的第二个技术核心。**21 条单次调用内置匹配规则**全部是**纯函数、无 IO、无 LLM**；跨调用的 EIT→PAT→NAT 序列由独立的有界工具链守卫处理，`default_deny` 是配置兜底，不混算为规则。组织策略还可用 JSON/YAML RE2 规则或预编译 OPA/Rego WASM 注入。

#### 规则分类体系：

| 优先级 | 类别 | 规则数 | 决策 | 示例 |
|--------|------|--------|------|------|
| 最高 | 安全不变式 (Invariants) | 3 | deny | 禁止工具、保护污点、数值范围 |
| 高 | 能力授权 (Capability) | 4 | deny | 工具不匹配、已过期、禁止污点、域名限制 |
| 高 | 阻断规则 (Deny) | 4 | deny | 凭据外发、危险 Shell、危险数据库变更 |
| 中 | 审批规则 (Review) | 8 | require_approval | 敏感数据外发、不可信来源/可变更工具 |

#### 规则执行示例：

**R5: 危险 Shell 模式检测（8种模式）**

```typescript
// engine.ts:196-205
const DANGEROUS_PATTERNS = [
  /curl ... | bash/,        // 远程下载即执行
  /wget ... | sh/,          // 同上
  /rm\s+.*-[rR].*[fF]/,    // 递归强制删除
  /chmod\s+777/,            // 全权限开放
  /eval\s+\S/,              // 动态执行
  />\s*\/dev\/(tcp|udp|...)/, // 设备重定向
  /mkfifo/,                 // 命名管道（常见于反弹shell）
  /nc\s+-[lL]/,            // netcat 监听模式
];
```

**R1: 敏感数据外发检测**

```typescript
// engine.ts:240-265
// 条件：send_email + 外部收件人 + 参数含 CUSTOMER_DATA/PII/FINANCIAL_DATA 等
// 决策：require_approval
```

#### 决策聚合逻辑：

```typescript
// engine.ts:766-767
const DECISION_ORDER = { deny: 3, require_approval: 2, allow: 1 };
const RISK_ORDER = { critical: 4, high: 3, medium: 2, low: 1 };
```

多条规则同时命中时，取**最严格的决策**和**最高风险等级**。

**面试要点**：这是典型的**规则引擎**实现。21 条单次调用基础规则用纯 TypeScript 函数
保持零配置和确定性；组织级 policy-as-code 可通过官方 OPA WASM runtime 单调扩展。
规则优先级（invariant > capability > deny > review）是有意设计的——安全不变式
绝对不可绕过，体现了纵深防御原则。

---

### 第四层：可扩展配置系统（[config.ts](packages/riskproof/src/config.ts)）

支持用户通过 JSON/YAML 文件定义**自定义规则**，无需修改源码：

```json
{
  "version": "1",
  "internalDomains": ["mycompany.com"],
  "options": { "defaultDecision": "deny" },
  "rules": [{
    "id": "block_prod_deploy",
    "tool": "shell_exec",
    "field": "command",
    "pattern": "deploy.*production",
    "decision": "deny",
    "risk": "critical",
    "consequence": "未经审核的生产环境变更可能导致宕机"
  }]
}
```

**技术选型考量**：

- 使用 **RE2JS**（Google RE2 的 JS 移植）而非原生 RegExp，保证正则匹配是**线性时间复杂度**，彻底防止 ReDoS 攻击
- 禁止 lookaround/backreference，确保匹配行为可预测
- 配置加载时即编译校验正则，fail-fast
- 支持 JSON（零依赖）和 YAML（可选依赖）

**安全设计**：配置加载器做了纵深防御——

- 1 MiB 文件大小限制
- 256 条规则上限
- 16 层嵌套深度上限
- 防止 Prototype Pollution（拒绝 `__proto__`、`constructor`、`prototype` 键）
- 拒绝 Proxy 对象和循环引用
- 拒绝未知字段（防止拼写错误如 `internalDomain` 单数形式）

---

### 第五层：运行时输入验证（[validation.ts](packages/riskproof/src/validation.ts)）

TypeScript 类型在运行时消失。所有 JSON-facing 入口都必须严格验证输入：

```typescript
// validation.ts 核心策略：
export function parseEngineInput(raw: unknown): EngineInput {
  // 1. 深度快照（snapshot）：递归克隆为 plain object
  //    拒绝 BigInt、Symbol、Proxy、getter、循环引用
  // 2. 结构化校验：拒绝未知字段、验证枚举值
  // 3. 复杂度限制：10k 节点、32 层深度、2 MiB 总字符
  // 4. 一致性校验：provenance/taint 引用的 key 必须在 args 中存在
  // 5. 防注入：拒绝包含 credential 模式的参数名
}
```

**面试要点**：这是**输入验证边界**（Input Validation Boundary）的完整实现。所有不可信数据在进入引擎前被规范化（normalize）和校验，实现了 **fail-closed** 原则——任何不合规的输入都被明确拒绝，而不会静默通过。这是一个安全系统的基本功。

---

### 第六层：风险解释生成器（[explainer.ts](packages/riskproof/src/explainer.ts)）

将结构化的 EngineOutput 转化为人类可读的审批卡片：

```
╔════════════════════════════════════════════════════════╗
║  🔴 BLOCKED                                           ║
╠════════════════════════════════════════════════════════╣
║  Action:  send_email                                  ║
║  Risk:    ██████████ CRITICAL                         ║
╠════════════════════════════════════════════════════════╣
║  ┌─ ARGUMENT CHAIN ────────────────────────────────   ║
║  │ to:                                                ║
║  │   external@evil.com                                ║
║  │   ⬅ untrusted_webpage                              ║
║  │   [UNTRUSTED] UNTRUSTED_WEB                        ║
║  │ body:                                              ║
║  │   [REDACTED:SECRET,API_KEY]                        ║
║  └────────────────────────────────────────────────────║
║  ┌─ PROTECTIONS TRIGGERED ─────────────────────────   ║
║  │ 🔴 secret_external_send                            ║
║  │   凭据/密钥通过邮件外发                                ║
║  └────────────────────────────────────────────────────║
║  ┌─ IF APPROVED, THIS COULD ────────────────────────  ║
║  │ • 凭据泄露到外部，可能被用于未授权访问                   ║
║  └────────────────────────────────────────────────────║
║  → REJECT — credentials detected in arguments         ║
╚════════════════════════════════════════════════════════╝
```

**设计理念**：

- 四个固定板块：① 参数链路 ② 触发的保护 ③ 可能后果 ④ 建议操作
- 每个匹配规则有预定义的 `label/detail/consequence` 数据库（RULE_DB）
- LLM 只负责表达润色，**不参与安全裁决**
- 技术细节：CJK 字符宽度计算、ANSI 转义序列清洗、敏感值脱敏展示

---

### 第七层：审计证明存储（[proof-store.ts](packages/riskproof/src/proof-store.ts)）

每次评估产出一个 **ProofRecord**，持久化到文件系统：

```
.riskproof/proofs/
  └── 2026-07/
      ├── rp_trace-abc_step-1_send_email_deny_secret_external_send_20260708T073714Z_a1b2c3d4.json
      └── ...
```

**安全措施**：

- 目录权限 `0700`，文件权限 `0600`
- 写入采用**原子提交**（写临时文件 → `linkSync` 硬链接，不覆盖已有文件）
- 读取时**完整性校验**：验证 record 摘要与 engineOutput 一致性，交叉校验 matchedRuleIds、evidence、reason
- 防篡改：任何字段不一致都会被标记为 corrupt record
- Proof ID 包含随机 UUID 后缀防碰撞

**面试要点**：这是审计追踪（Audit Trail）的完整实现，支持安全审计、攻击复盘、合规报告。原子提交保证了并发写入的安全性。

---

### 第八层：多接入模式

| 模式 | 实现文件 | 用途 |
|------|----------|------|
| **CLI `check`** | [cli.ts](packages/riskproof/src/cli.ts) | 单次评估，支持 Claude Code 事件格式自动转换 |
| **HTTP Server** | [http-server.ts](packages/riskproof/src/http-server.ts) | 语言无关的 HTTP sidecar，允许 Python/Go 等语言调用 |
| **MCP Proxy** | [proxy-server.ts](packages/riskproof/src/proxy-server.ts) | 透明代理，拦截 MCP 协议的 tools/call 并评估 |
| **JS API** | [index.ts](packages/riskproof/src/index.ts) | 直接 `import { evaluate } from "riskproof"` 调用 |

#### MCP Proxy 的独特能力

- **Schema 投毒检测**：扫描上游 MCP 工具的 description 和 inputSchema，检测 prompt injection 模式
- **工具隐藏**：被投毒的工具从 agent 可见的 `tools/list` 响应中移除
- **交互式审批**：在 TTY 上展示审批卡片，等待用户 [A]pprove / [R]eject

---

## 三、技术策略总结

### 3.1 架构策略

```
                    ┌──────────────────────────┐
                    │   Tool Call Interceptor   │  ← 拦截层
                    └──────────┬───────────────┘
                               ▼
                    ┌──────────────────────────┐
                    │   Provenance + Taint      │  ← 数据流分析层
                    └──────────┬───────────────┘
                               ▼
                    ┌──────────────────────────┐
                    │ Policy Engine (21 rules) │  ← 单次调用确定性策略层
                    └──────────┬───────────────┘
                               ▼
              ┌────────────────┼────────────────┐
              ▼                ▼                 ▼
           allow          ask_approval         block
              │                │                 │
              ▼                ▼                 ▼
         Tool Executes    Human Decides     Action Stopped
              │                │                 │
              └────────────────┼─────────────────┘
                               ▼
                    ┌──────────────────────────┐
                    │   Proof Store (Audit)     │  ← 审计层
                    └──────────────────────────┘
```

### 3.2 关键设计原则

| 原则 | 体现 |
|------|------|
| **确定性** | 策略引擎纯函数，无随机、无 IO、无 LLM 做安全裁决 |
| **Fail-Closed** | 未知工具、非法输入、验证失败 → 拒绝而非静默放行 |
| **纵深防御** | 不变式 → 能力 → 阻断规则 → 审批规则，层层递进 |
| **受控依赖** | `re2js` 提供线性时间正则，官方 OPA WASM runtime 提供本地 Rego 执行 |
| **可审计** | 每次决策产生结构化 Proof JSON，持久化存储 |
| **可扩展** | 自定义规则通过 JSON/YAML 启动配置或 JS API 注入；组织规则可用 OPA WASM |

### 3.3 安全场景覆盖

22 个 Fixture 覆盖了 5 大类攻击场景：

1. **Schema 投毒**：工具描述被注入恶意指令
2. **危险命令执行**：curl\|bash、rm -rf、chmod 777、eval、nc -l、mkfifo 反弹shell、设备重定向、wget\|sh
3. **数据外泄**：API Key / Token / 客户数据 / 财务数据 / 患者数据发往外部
4. **间接 Prompt Injection**：不可信网页/邮件内容影响工具参数
5. **企业场景**：安全审计锁定、临床数据保护、批量操作阈值

---

## 四、面试项目该怎么写

### 简历 Bullet Points（建议 3-4 条）

```
RiskProof — AI Agent 高风险工具调用风险感知批准层 | TypeScript, Node.js

• 设计并实现了一个确定性策略引擎，包含 21 条单次调用内置安全规则，覆盖凭据外泄、
  间接 prompt injection、危险命令执行等 5 大类攻击场景，决策完全确定
  性、零 LLM 依赖

• 实现了参数级 Provenance 追踪 + Taint 污点分析系统：通过来源推断、值
  检测、标签传播三层机制自动识别参数风险，借鉴 Information-Flow Control
  学术思路

• 构建了完整的审计证明链条：每次工具调用产生结构化 Proof JSON，支持
  原子提交、完整性校验、防篡改，适配安全审计和合规需求

• 提供了多接入模式（CLI / HTTP Sidecar / MCP Proxy / JS API），支持
  LangGraph、OpenAI Agents SDK 等主流 Agent 框架集成
```

### 面试话术模板（按 STAR 法则）

**Situation（背景）**：

> 当前企业级 AI Agent 正在从"只生成文本"走向"调用真实工具"——发邮件、调 API、执行命令。但现有审批机制只告诉用户"Agent 要做什么"，不告诉用户"参数从哪来、有什么风险、批准后可能什么后果"，human approval 容易变成橡皮图章。

**Task（目标）**：

> 做一个可插拔的安全组件，在 Agent 高风险工具调用执行前，提供参数级来源追踪、污点分析、策略匹配和后果解释，让用户在充分知情的情况下做审批决策。

**Action（行动）**：

> - 设计了 EngineInput → EngineOutput 的单一数据流模型，以 TypeScript 显式类型约束
> - 实现了三层污点分析：来源关键词推断 + 参数值模式检测 + 声明标签合并
> - 构建了 21 条单次调用确定性策略规则，并以独立有界状态守卫识别跨工具 EIT→PAT→NAT 序列；策略按 invariant > capability > deny > review 优先级排序，并支持 OPA/Rego WASM 单调扩展
> - 规则引擎使用 RE2JS（Google RE2）保证正则匹配线性时间复杂度，防止 ReDoS
> - 设计了分层安全边界：输入验证边界（2 MiB / 10k 节点限制、防原型污染）→ 引擎边界 → 存储边界
> - 输出了结构化审计证明 + 人类可读风险解释卡片

**Result（成果）**：

> - 当前合成 Fixture 中，攻击场景均按预期阻断/审批，良性对照均按预期放行
> - 这证明固定回归集符合预期；尚未建立真实业务分布上的误报率、漏报率和攻击成功率基线
> - 支持 JSON/YAML 配置自定义规则，无需修改源码
> - 暴露 HTTP sidecar 和 MCP Proxy 接口，可被 Python/Go 等多语言调用

---

## 五、常见面试追问及回答

### Q1: 为什么同时保留内置规则和 OPA/Rego？

> 21 条单次调用基础安全规则必须零配置、确定性地执行，所以保留纯函数实现；跨调用序列由工具链守卫维护有界元数据状态；组织级策略通过官方 OPA WASM runtime 注入。聚合是单调的：OPA 只能收紧而不能降低内置决策，运行错误或非法结果默认 fail-closed。这样兼顾开箱即用和真正 policy-as-code，而不是让部署 OPA 成为基本安全能力的前置条件。

### Q2: Provenance 追踪的数据从哪来？

> MCP Proxy 自动截获 resource、prompt 和工具返回，通过精确子串反查参数来源；直接 API 调用也可显式声明 provenance。模型摘要导致原文消失时，可信集成用单调 `flows` 声明派生关系。系统不使用模糊相似度猜测安全来源，因为误关联会造成错误信任。

### Q3: 污点分析会不会误标？

> 存在这个风险。关键词匹配（如 "customer" 触发 CUSTOMER_DATA）可能误标。缓解策略是分层：值检测的污点用于规则匹配，但最终决策还有 capability 和 invariant 两道防线。用户也可以在配置中自定义规则覆盖误判场景。

### Q4: 和普通 guardrail 有什么区别？

> Guardrail 看的是"输入/输出是否违规"，RiskProof 看的是"工具参数从哪来、携带什么风险标签、会导致什么后果"。两者互补：guardrail 保护对话安全，RiskProof 保护工具执行安全。

### Q5: 性能开销多大？

> 策略引擎是纯内存计算，单次 evaluate 在微秒级。主要开销在输入验证的 JSON snapshot（O(n) 遍历）。整体延迟远小于 LLM 推理延迟（通常秒级），不会成为瓶颈。

### Q6: 为什么选择 RE2JS 而不是原生 RegExp？

> 原生 JavaScript RegExp 支持 backreference 和 lookaround，可能被恶意正则触发 ReDoS（正则拒绝服务攻击），导致引擎卡死。RE2 保证线性时间复杂度，这是安全系统的硬性要求。同时，禁止 lookaround/backreference 也使得自定义规则的行为在任何输入下都可预测。

### Q7: 项目中有哪些体现"纵深防御"的设计？

> 1. 输入验证层：2 MiB / 10k 节点限制，防止资源耗尽
> 2. 污点分析层：三层机制互补（声明 + 推断 + 检测）
> 3. 策略引擎层：不变式（不可绕过）→ 能力检查 → 阻断规则 → 审批规则
> 4. 输出脱敏层：边界处自动 redact 敏感值
> 5. 存储安全层：目录 0700、文件 0600、原子提交、完整性校验

### Q8: 如果让你重新设计，你会改进什么？

> 1. 将显式 `flows` 进一步集成到 Agent 框架的 context 构建与摘要算子中
> 2. 接入结构化 DLP/数据目录，降低纯模式检测的误报和漏报
> 3. 审批 Token 使用非对称、短期、单次签名，而非 unsigned metadata
> 4. Proof HMAC 升级为外部 KMS/HSM 管理的公开可验证签名与远程不可变账本
> 5. 为批准后的 Shell/数据库操作提供独立 sandbox 和最小权限执行器

---

## 六、项目亮点一句话总结

这个项目最打动面试官的点应该是：

> **你不是做了一个"大而全"的 Agent 安全平台，而是在一个极窄但极硬的问题上——"用户在批准高风险工具调用前，是否真正理解这个操作的来源、风险和后果"——做出了完整的、可审计的、确定性的技术闭环。**

从自动参数级 provenance 追踪，到 21 条单次调用确定性规则、工具链序列守卫与 OPA/Rego 扩展，再到加密/验签 Proof 审计链和多接入模式，每一步都有明确的技术决策理由和学术/工业借鉴来源。安全工程重点体现在输入验证、纵深防御、fail-closed、原子存储、加密与防篡改校验，而不是堆叠普通 CRUD 功能。
